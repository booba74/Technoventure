using UnityEngine;
using UnityEngine.UI;

public class PowerBarController : BarController
{
    protected override void InitBar(float maxCount)
    {
    //    bar = GameObject.FindWithTag("powerBar");
  //      barText = GameObject.FindWithTag("powerBarText").GetComponent<Text>();
  //      base.InitBar(_maxCount);
    }
}
